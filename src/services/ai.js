async function generateResponse(userMessage) {
  // TODO: Integrate with AI API
  // For now, skip AI and return null to signal processor to use fallback
  return null;
}

module.exports = { generateResponse };
